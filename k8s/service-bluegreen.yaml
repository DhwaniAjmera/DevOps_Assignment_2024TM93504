apiVersion: v1
kind: Service
metadata:
  name: aceest-service
spec:
  type: NodePort
  selector:
    app: aceest
    version: blue   # change to green to switch traffic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007